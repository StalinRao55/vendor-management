# Production Database Configuration (PostgreSQL with Aiven)
# This profile is used when connecting to Aiven PostgreSQL database

# Include Aiven Database Configuration
spring.config.import=database.properties

# Server Configuration
server.port=8080

# Security Configuration
spring.security.enabled=true

# Disable H2 Console (not needed for PostgreSQL)
spring.h2.console.enabled=false